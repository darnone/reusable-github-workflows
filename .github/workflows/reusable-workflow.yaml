name: Remote Reausable Workflow

on:
  workflow_call:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Write soemthing to log
          echo "I am a resuable workflow - main branch!"